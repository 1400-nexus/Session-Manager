[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "session-manager"
version = "0.1.0"
description = "Nexus RX-side session manager: aggregates receiver progress, verifies transfers, publishes output"
requires-python = ">=3.11"
dependencies = [
    "blake3",
    "inotify-simple",
    "structlog",
    "protobuf",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "hypothesis",
    "mypy",
    "ruff",
    "types-protobuf",
]

[project.scripts]
session-manager = "session_manager.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/session_manager"]

[tool.hatch.build.targets.wheel.force-include]
"libs/nexus-proto/generated/python" = "."

[tool.ruff]
line-length = 100
extend-exclude = ["libs/nexus-proto"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
strict = true
mypy_path = "src"
explicit_package_bases = true
platform = "linux"

[[tool.mypy.overrides]]
module = "inotify_simple"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["common_pb2", "net_pb2", "ipc_pb2", "rx_pb2"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
